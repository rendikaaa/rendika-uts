name: catatan_sederhana
description: A simple note-taking application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.0.0+4 # Pastikan untuk menambahkan ini
  path: ^1.8.0 # Jika Anda menggunakan path untuk menyimpan database

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true